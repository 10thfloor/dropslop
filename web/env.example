# Local development environment variables (frontend)
#
# Usage:
#   cp env.example .env.local
#
# Notes:
# - Next.js automatically loads `.env.local` in development.
# - Leave these blank to use same-origin `/api/*` and `/events/*` (recommended).

# Optional override for API proxy target (e.g., if running API elsewhere)
NEXT_PUBLIC_API_URL=

# Optional override for direct SSE connection (local dev default is http://localhost:3004)
NEXT_PUBLIC_SSE_URL=


